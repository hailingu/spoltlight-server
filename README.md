# spoltlight-server

This is a backend for the splotlight project

## Install dependency

    pip install scikitlearn
    pip install pandas
    pip install numpy
    pip install scipy
    pip install flask
    pip install flask_cors
    pip install pyspark